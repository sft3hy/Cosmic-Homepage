apiVersion: v1
kind: Service
metadata:
  name: {{ include "cosmic-homepage.fullname" . }}
  labels:
    {{- include "cosmic-homepage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cosmic-homepage.selectorLabels" . | nindent 4 }}
